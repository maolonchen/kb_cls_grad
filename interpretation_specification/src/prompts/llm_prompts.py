def LLM_POST_SIMILAR_PROMPT(ori_data, similar_data):
    return (f"""你是一个数据判别专家。需要对 **”原始数据”**：`{{{ori_data}}}` 和 **”经过相似度比较后的数据”**：`{{{similar_data}}}` 进行判断。

### 判断逻辑和要求：

1. **第一步：精确匹配**
   从”原始数据”中提取标识符（例如 `[C1-2]` 中的 `C1-2`），在”经过相似度比较后的数据”中查找以该标识符开头的项（忽略后续描述）。
   示例：若”经过相似度比较后的数据”包含 `{{“C1-2 服务记录和日志”, “<等级>”}}`，则 `C1-2` 匹配成功，获取其对应等级。

2. **第二步：首字母匹配（仅当无精确匹配时）**
   若未找到精确匹配，则取”原始数据”标识符的**第一个字母**（如 `[Z7]` → `Z`），在”经过相似度比较后的数据”中查找每项**去除前导空格后第一个非空格字符**是否等于该字母。
   - 示例：`[Z7]` 与 `{{“Z W9 网络与系统资源类数据”, “<等级>”}}` 首字符均为 `Z`，匹配成功。
   - **注意**：此阶段**只比对第一个字母**，忽略后续所有内容（包括数字、空格、其他字母等）。

3. **第三步：次字母匹配（仅当首字母完全无匹配时）**
   如果在”经过相似度比较后的数据”中**没有任何一项的首字母**与原始数据首字母匹配，则退而使用原始数据标识符的**第一个字母**（仍为 `Z`），去匹配目标项中的**第二个有效字母**（即跳过首字母和其后的空格/数字/符号，取下一个英文字母）。
   - 示例：对于 `{{“Z W9 网络与系统资源类数据”, “<等级>”}}`，其结构为 `Z W9...`，首字母是 `Z`，第二个有效字母是 `W`。
   - 此时，仅当原始数据为 `[W...]` 且首字母 `W` 在所有项的首字母中都未匹配时，才允许用 `W` 去匹配该项的第二个有效字母 `W`。
   - **关键限制**：次字母匹配**仅在首字母全局无任何匹配时启用**，且只匹配目标字符串中**第二个出现的英文字母**（忽略中间的数字、空格、中文等）。

4. **第四步：多结果合并**
   遍历所有匹配成功的项（可能因条件不同存在多个）。若描述中包含括号（如 `”G6-9 招标数据（XXX）”`），则提取括号内内容作为”条件”；否则”条件”为空。
   将所有匹配项的”等级”和”条件”分别按顺序用 `/` 拼接，合并为单一对象。

### 输出格式要求：

- 输出必须是**长度为1的 JSON 对象数组**：
  - **无条件**：`[{{“等级”: “<等级>”, “条件”: “”}}]`
  - **有条件**：`[{{“等级”: “<等级1>/<等级2>”, “条件”: “XXX/YYY”}}]`
  - **无匹配**：`[{{“等级”: “”, “条件”: “”}}]`
- **禁止添加任何额外说明，仅输出 JSON 数组。**

### 特别说明：
- 原始示例 `”Z W9 网络与系统资源类数据, “<等级>””` 必须保留原样。
- “第二个有效字母”指字符串中**第二个出现的英文字母**（A–Z，大小写不敏感），跳过非字母字符。
- 次字母匹配是**兜底策略**，优先级低于首字母匹配，且仅在首字母完全无匹配时激活。
- 必须输出一个等级。"""
    )
    
    
def FEATURE_ENTITY_PROMPT(feature_data):
    return (f'''请从以下文本中提取所有**具体且可独立管理的数据单元名称**。

核心原则：
- 必须避免提取孤立的泛化词（如“类型”“数据”“信息”“内容”）
- 如果多个并列项共享一个前缀（如“采购物资”），必须将前缀补全到每一项，形成完整字段名

具体规则：
1. **识别并列结构**：
- 找到由“、”“，”“和”“与”分隔的多个项
- 特别注意：如果第一项包含一个明确的前缀（如“采购物资”），后续项是单个词（如“数量”“类型”），则后续项应继承该前缀

2. **执行前缀补全**：
- 示例：
    - “采购物资数量、类型” → 补全为：采购物资数量、采购物资类型
    - “用户登录时间、IP、设备” → 补全为：用户登录时间、用户登录IP、用户登录设备
- 补全后的字段必须是原文语义的合理延伸

3. **处理括号与缩写**：
- “缩写（中文）” → 提取缩写（如 DDM）
- “分类（A、B）” → 提取 A、B

4. **过滤泛化词**：
- 禁止输出：类型、数据、信息、内容、相关、等、方面、过程、服务、功能
- 即使原文有，也不得单独输出
- 除非它是复合词的一部分（如“物资类型”可，但“类型”不可）

5. **标点与引导词**：
- “包括A、B” → 提取 A、B
- “A：X、Y” → 提取 X、Y

输出要求：
- 仅返回涉及到的n个字段名，严禁字段是一段话！
- 格式：["字段1", "字段2", "字段3", "字段4", ...]
- 输出格式：纯JSON对象数组，不包含额外说明

文本："{feature_data}"
'''
    )
    
def CONTENT_TO_GRAD_PROMPT(post_data):
    """构造模型提示"""
    return (
        '你是数据识别和处理专家。请根据输入的数据进行处理：\n'
        '要求：\n'
        '1.判断输入的数据中是否有分类的等级信息（分类的等级特征一般由类别即字母和类别等级数字和"-"组成，例如："""T8-4 太空信息：飞船数据、人体数据"""，注意T8-4只是案例，实际可能会有多级（即多个"-"）\n'
        '2.如果有返回1和等级信息（格式：等级 空格 后面的文字简短总结），没有则返回0\n'
        '3.如果有多个分类，请用分号";"分隔每个分类\n'
        '4.返回格式是严格的纯json，不要有其他任何说明\n'
        '5.注意json的花括号，不是圆括号!\n'
        '6.单引号要统一\n'
        '案例如下：\n'
        '{"1":"T8-4 太空数据"}'
        '或\n'
        '{"1":"T8-4 太空数据; T8-5 地面数据; T8-6 海洋数据"}'
        '或\n'
        '{"0":""}'
        '以下是实际输入：\n'
        f'{{{post_data}}}'
    )
    
def PRIORITY_SORT_PROMPT(grades):
    return (f"""
你是一个数据安全专家，需要对以下数据等级按照重要性进行排序。

排序规则：
1. 名称中包含类似"核心"或"重要"等其他关键词的等级具有最高优先级
2. 对于名称中包含的数字的等级（如"第1级"、"第2级"等），等级中数字越大，优先级越高，Priority越小
3. 如果两个等级都包含"第"字和数字，等级中数字越小的优先级更高，Priority越大
4. 如果两个等级都不包含"第"字和数字，则根据名称中的数字判断，等级中数字大的优先级更高，Priority越小
5. Priority的数字越小表示越重要，例如最重要等级优先级为1

请将以下等级按照上述规则进行排序，并为每个等级分配一个优先级数字：

等级列表：{', '.join(grades)}

请以JSON格式返回结果，格式如下：
[
  {{"DataGrading": "等级名称A", "Priority": 1}},
  {{"DataGrading": "等级名称B", "Priority": 2}},
  {{"DataGrading": "等级名称C", "Priority": 3}},
  ...

]

只返回JSON数组，不要包含其他文字。
""")